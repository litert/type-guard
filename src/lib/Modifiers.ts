import * as C from "./Common";

export const PREFIX: string = C.MODIFIER_PREFIX;

export const NOT: string = `${C.MODIFIER_PREFIX}not`;

export const OR: string = `${C.MODIFIER_PREFIX}or`;

export const AND: string = `${C.MODIFIER_PREFIX}and`;

export const MAP: string = `${C.MODIFIER_PREFIX}map`;

export const LIST: string = `${C.MODIFIER_PREFIX}list`;

export const ARRAY: string = `${C.MODIFIER_PREFIX}array`;

export const DICT: string = `${C.MODIFIER_PREFIX}dict`;

export const STRICT: string = `${C.MODIFIER_PREFIX}strict`;

export const STRING: string = `${C.MODIFIER_PREFIX}string`;

export const TUPLE: string = `${C.MODIFIER_PREFIX}tuple`;

export const EQUAL: string = `${C.MODIFIER_PREFIX}equal`;

export const TAG: string = `${C.MODIFIER_PREFIX}tag`;

export const TYPE: string = `${C.MODIFIER_PREFIX}type`;
